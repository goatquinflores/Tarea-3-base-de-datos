<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 20px;
      background: linear-gradient(135deg, #f0f4c3, #e3f2fd);
      color: #333;
    }
    h1 {
      text-align: center;
      color: #2e7d32;
      margin-bottom: 20px;
    }
    table {
      margin: 0 auto;
      border-collapse: collapse;
      width: 80%;
      max-width: 800px;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }
    table thead tr {
      background: #2e7d32;
      color: white;
    }
    table td, table th {
      padding: 15px 20px;
      text-align: left;
    }
    table tbody tr:nth-child(odd) {
      background: #f9f9f9;
    }

    table tbody tr:nth-child(even) {
      background: #e8f5e9;
    }
    table td:nth-child(1), table th:nth-child(1) {
      padding-right: 40px; 
    }

    table td:nth-child(2), table th:nth-child(2) {
      padding-left: 40px; 
    }
  </style>
</head>
<body>
  <h1>Stock de productos</h1>
  <table>
    <thead>
      <tr>
        <th>Producto</th>
        <th>Stock</th>
      </tr>
    </thead>
    <tbody>
      {{#each productos}}
        <tr>
          <td>{{nombre}}</td>
          <td>{{stock}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</body>
</html>
