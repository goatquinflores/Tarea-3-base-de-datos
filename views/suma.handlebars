<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #e3f2fd, #f0f4c3);
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      background: #ffffff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 500px;
      width: 100%;
    }
    h1 {
      font-size: 2em;
      color: #2e7d32;
      margin-bottom: 20px;
    }
    p {
      font-size: 1.2em;
      line-height: 1.5;
      margin-bottom: 30px;
      color: #424242;
    }
    p span {
      font-weight: bold;
      color: #2e7d32;
    }
    button {
      background: #2e7d32;
      color: white;
      border: none;
      padding: 12px 20px;
      font-size: 1em;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s, transform 0.2s;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    button:hover {
      background: #1b5e20;
      transform: scale(1.05);
    }

    button:active {
      background: #4caf50;
      box-shadow: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>{{title}}</h1>

    <p>Total de stock de todos los productos: <span>{{totalStock}}</span></p>

    <button onclick="window.location.href='/productos'">Volver a Productos</button>
  </div>
</body>
</html>
